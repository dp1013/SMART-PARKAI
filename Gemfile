source 'https://rubygems.org'

gem 'stripe', '~> 15.0'
gem 'sinatra'  # For creating a simple web server
gem 'dotenv'   # For managing environment variables
gem 'json'     # For JSON parsing 

# Stripe API Configuration
STRIPE_SECRET_KEY=sk_test_...  # Replace with your NEW secret key after rotation
STRIPE_PUBLISHABLE_KEY=pk_test_...  # Replace with your publishable key
STRIPE_WEBHOOK_SECRET=whsec_...  # Replace with your webhook secret

# Stripe Connect Account (if using Connect)
STRIPE_ACCOUNT_ID=acct_1032D82eZvKYlo2C

# Debug mode (optional)
STRIPE_DEBUG=true 